/**
 * Server-safe PageDocument schema entry point. Keep it independent from the
 * editor's React and Puck exports so Route Handlers can validate documents.
 */
export * from "../../../src/core/schema/page-document";
