# Shopify UI boundary

This directory is reserved for adapters to Polaris and App Bridge. Builder-specific
layout, tokens and overlays live in `../builder`; neither concern is part of the
public PageDocument or extension contracts.
